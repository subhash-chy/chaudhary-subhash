const primary = "#1d4ed8";
// const primary = "#00e472";
// #06d6a0
// #1d4ed8
// #6a00ff
// const colors = ["#1d4ed8", "#00e472"];
// // let primary = colors[Math.floor(Math.random() * colors.length)];

// if (typeof window !== "undefined") {
//   // browser code
//   window.onload = function (colors) {
//     generateRandomColor(colors);
//   };
// }

// function generateRandomColor(colors) {
//   return colors[Math.floor(Math.random() * colors.length)];
// }

// const primary = generateRandomColor(colors);
// console.log("primary ", primary);

module.exports = {
  primary,
};
